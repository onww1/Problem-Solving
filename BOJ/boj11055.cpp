#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	cin.tie(NULL);
	ios_base::sync_with_stdio(false);

	int N, A[1000];
	cin >> N;
	for (int i=0; i<N; i++)
		cin >> A[i];

	int prev = 0, sum = 0;
	

	return 0;
}